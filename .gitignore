# Claude planning artifacts (per global user policy — never commit)
.claude/plans/
.claude/specs/

# Personal scan data (developer-local fixtures, not for public commit)
sample-scans/

# Local screen captures used to triage user-reported bugs; not for commit
screen-captures/

# Starting-point reference materials (personal handoff docs from project
# kickoff; the implementation has long since superseded them and they're
# kept locally for reference only — not for public commit).
HANDOFF.md
deeper_analysis.py
lake_full_analysis.png
sweet_spots.csv

# Environment files (defensive — we don't use them but standard hygiene)
.env
.env.*
!.env.example

# Common
.DS_Store

# Node
node_modules/
dist/
coverage/
*.log
.vite/
*.tsbuildinfo

# Editor
.vscode/
.idea/

# Python (deeper_analysis.py reference + scripts/build-equivalence-fixture.py)
__pycache__/
*.pyc

# Playwright outputs
test-results/
playwright-report/
e2e/.auth/
